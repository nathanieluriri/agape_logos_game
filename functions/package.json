{
  "name": "functions",
  "private": true,
  "main": "lib/index.js",
  "engines": {
    "node": "22"
  },
  "scripts": {
    "build": "tsc",
    "lint": "eslint --ext .ts --no-error-on-unmatched-pattern src test",
    "test": "firebase emulators:exec --only auth,firestore \"jest --runInBand\"",
    "deploy": "firebase deploy --only functions",
    "seed": "npm run build && firebase emulators:exec --only firestore \"node lib/scripts/seed.js\"",
    "extend": "npm run build && firebase emulators:exec --only firestore \"node lib/scripts/extend.js\"",
    "clean:defs": "npm run build && firebase emulators:exec --only firestore \"node lib/scripts/clean_definitions.js\"",
    "purge:pool": "npm run build && firebase emulators:exec --only firestore \"node lib/scripts/purge_pool.js\"",
    "stats": "npm run build && firebase emulators:exec --only firestore \"node lib/scripts/stats.js\"",
    "export:starter": "npm run build && firebase emulators:exec --only firestore \"node lib/scripts/export_starter_pack.js\""
  },
  "dependencies": {
    "@asteasolutions/zod-to-openapi": "^7.3.0",
    "cors": "^2.8.5",
    "express": "^4.21.2",
    "firebase-admin": "^13.0.2",
    "firebase-functions": "^6.3.2",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/cors": "^2.8.17",
    "@types/express": "^4.17.21",
    "@types/jest": "^29.5.14",
    "@types/supertest": "^6.0.2",
    "@typescript-eslint/eslint-plugin": "^7.18.0",
    "@typescript-eslint/parser": "^7.18.0",
    "eslint": "^8.57.1",
    "jest": "^29.7.0",
    "supertest": "^7.0.0",
    "ts-jest": "^29.2.5",
    "typescript": "^5.7.3"
  }
}
